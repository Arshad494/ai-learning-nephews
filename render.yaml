services:
  - type: web
    name: ai-learning-nephews
    runtime: python
    plan: free
    repo: https://github.com/Arshad494/ai-learning-nephews
    branch: main
    buildCommand: bash build.sh
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: "20"
      - key: PYTHON_VERSION
        value: "3.11.0"
